---
# Path to a directory in which to create a virtualenv.
os_openstackclient_venv:

# Whether to install EPEL repository package.
os_openstackclient_install_epel: True

# Whether to install package dependencies.
os_openstackclient_install_package_dependencies: True

# Version of python-openstackclient to install, or latest if empty.
os_openstackclient_version:
